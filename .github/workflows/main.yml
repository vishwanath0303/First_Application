name: Docker Image CI

on:
  workflow_dispatch:
    inputs:
      image_name:
      type: string
      default: 'vishwa'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Build the Docker image
      run: echo "Hello"